*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# Committed (not built server-side): this cPanel account has no working
# Node.js Selector (the UAPI NodeJS module isn't installed), so
# .cpanel.yml's `npm run build` step warns and no-ops on every deploy.
# Run `npm run build` locally and commit the output before pushing any
# change to resources/css or resources/js.
# /public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
